description "Jupyter notebook process"

start on (local-filesystems and net-device-up IFACE=eth0)
stop on runlevel [!12345]

respawn

setuid root
setgid root

exec jupyter notebook --ip 0.0.0.0 --no-browser --notebook-dir /usr/local/share/jupyter/notebook
